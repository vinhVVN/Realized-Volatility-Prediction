from setuptools import setup, find_packages

setup(
    name="optiver_volatility",
    version="0.1.0",
    description="Optiver Realized Volatility Prediction Solution",
    author="Quantitative Researcher",
    packages=find_packages(),
    install_requires=[],
)
